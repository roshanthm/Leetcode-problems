def rotate(nums, k):
    # If k is larger than the length of the list, take remainder
    k = k % len(nums)

  
    nums[:] = nums[-k:] + nums[:-k]

nums = [1, 2, 3, 4, 5, 6, 7]

rotate(nums, 3)


print(nums)
